// This module is used for writing unit tests for node applications.
// require('assert')

const as = require('assert')
const { assert } = require('console')

// Throws an exception that displays the values for actual and expected seperated by the provided operator.

as.fail()